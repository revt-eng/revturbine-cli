node_modules/
dist/
*.log
.DS_Store

# Operator credentials — a token must never be committed (see revt-config login).
.revturbine/
.env
.env.*

# Track graph metadata only; keep other local credentials/state ignored.
!/.revturbine/
/.revturbine/*
!/.revturbine/graph-bindings.json
