# CLAUDE.md

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This file exists only to pull
the shared instructions in; all guidance lives in `AGENTS.md`, which every
runtime shares — and whose first line imports the devkit rules every repo
inherits. Add rules there, never here.

@AGENTS.md
